# 🚀 Инструкция по деплою на Beget

## ✅ Build готов!

Ваш проект собран и находится в папке `/dist/`

---

## 📋 Шаг 1: Подготовка

### Что загружать на хостинг:

Нужно загрузить **ВСЁ содержимое папки `dist/`**:

```
dist/
├── index.html              ← главная страница
├── .htaccess              ← настройки для Apache (уже создан)
├── assets/                ← JS, CSS файлы
│   ├── index-xxxxxxx.js
│   └── index-xxxxxxx.css
└── images/                ← картинки, иконки
    └── ...
```

---

## 📤 Шаг 2: Загрузка на Beget

### Способ 1: Через File Manager (веб-интерфейс)

1. Зайдите на https://beget.com
2. Войдите в панель управления
3. Откройте **File Manager** (Файловый менеджер)
4. Перейдите в папку вашего сайта (обычно `public_html` или `название_домена`)
5. **Удалите всё** что там есть (старые файлы)
6. Загрузите **ВСЁ содержимое папки `dist/`**
7. ✅ Готово!

### Способ 2: Через FTP

1. Скачайте FTP клиент (например FileZilla)
2. Подключитесь к Beget:
   - **Хост:** `название_вашего_аккаунта.beget.tech`
   - **Порт:** 21
   - **Логин:** ваш логин на Beget
   - **Пароль:** ваш пароль от Beget
3. Перейдите в папку вашего сайта
4. Удалите старые файлы
5. Загрузите всё из папки `dist/`

### Способ 3: Через SSH (для продвинутых)

```bash
# 1. Архивируем dist
cd /Users/eldar/planeta
tar -czf planeta-dist.tar.gz -C dist .

# 2. Загружаем на сервер
scp planeta-dist.tar.gz логин@название.beget.tech:~/

# 3. Подключаемся по SSH
ssh логин@название.beget.tech

# 4. Распаковываем в public_html
cd ~/public_html/название_домена
rm -rf *  # удаляем старые файлы
tar -xzf ~/planeta-dist.tar.gz
rm ~/planeta-dist.tar.gz
```

---

## 🔧 Шаг 3: Настройка на Beget

### 3.1 Проверка .htaccess

Файл `.htaccess` уже создан в папке `dist/`. Убедитесь что он загружен!

Если файла нет - создайте вручную через File Manager с таким содержимым:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

### 3.2 Настройка домена

1. В панели Beget откройте **Сайты**
2. Выберите ваш домен
3. Убедитесь что **Document Root** указывает на папку с файлами
4. Если нужно - настройте SSL сертификат (бесплатно от Let's Encrypt)

### 3.3 Права доступа

Обычно Beget автоматически ставит правильные права, но если что:
- Папки: `755`
- Файлы: `644`

---

## 🌐 Шаг 4: Проверка работы

### Откройте ваш сайт в браузере

```
http://ваш-домен.ru
```

### Что должно работать:

- ✅ Главная страница открывается
- ✅ React Router работает (переходы между страницами)
- ✅ Прямые ссылки работают (например `/signin`, `/clients`)
- ✅ Страница 404 не показывается на существующих роутах

### Если что-то не работает:

**Проблема: "Not Found" или 404 на роутах**

Решение: Проверьте что файл `.htaccess` загружен и содержит правила для React Router.

**Проблема: Белый экран**

1. Откройте DevTools Console (F12)
2. Посмотрите на ошибки
3. Обычно это ошибки загрузки JS/CSS файлов

Решение: Проверьте что **все файлы** из `dist/assets/` загружены на сервер.

**Проблема: API не работает (Failed to fetch)**

Это нормально! API работает только после настройки CORS на бэкенде.

Временно можно использовать моковые данные (через STOContext).

---

## 🔐 Шаг 5: SSL сертификат (HTTPS)

Рекомендуется настроить HTTPS:

1. В панели Beget → **SSL**
2. Выберите домен
3. Нажмите **Установить Let's Encrypt**
4. Подождите 5-10 минут
5. ✅ Сайт будет доступен по HTTPS

После установки SSL обновите настройки:

```apache
# В .htaccess добавьте редирект на HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
```

---

## 🎯 Шаг 6: Настройка API

После деплоя фронтенда нужно настроить подключение к API:

### Если бэкенд на другом домене:

Убедитесь что в бэкенде (Rails) настроен CORS:

```ruby
# config/initializers/cors.rb
Rails.application.config.middleware.insert_before 0, Rack::Cors do
  allow do
    origins 'https://ваш-домен.ru'
    resource '*',
      headers: :any,
      methods: [:get, :post, :put, :patch, :delete, :options, :head],
      expose: ['Authorization'],
      credentials: true
  end
end
```

### Если нужно изменить URL API:

Отредактируйте `src/lib/apiClient.ts` перед сборкой:

```typescript
const BASE_URL = 'https://ваш-бэкенд.ru/api/v1';
```

И пересоберите:
```bash
npm run build
```

---

## 📊 Оптимизация (опционально)

### Кэширование

Файл `.htaccess` уже содержит правила кэширования.

### Сжатие GZIP

Тоже уже настроено в `.htaccess`.

### Мониторинг

В Beget есть статистика посещений - посмотрите в разделе **Статистика**.

---

## 🔄 Обновление сайта

Когда нужно обновить сайт:

1. Внесите изменения в код
2. Соберите новый build:
   ```bash
   npm run build
   ```
3. Загрузите содержимое `dist/` на сервер (замените старые файлы)
4. Очистите кэш браузера (Ctrl+F5)

---

## 📝 Checklist перед деплоем

- [x] Build собран (`npm run build`)
- [x] Папка `dist/` создана
- [x] Файл `.htaccess` добавлен
- [ ] Все файлы загружены на Beget
- [ ] Домен настроен и указывает на папку с файлами
- [ ] SSL сертификат установлен (рекомендуется)
- [ ] Сайт открывается в браузере
- [ ] React Router работает (прямые ссылки)
- [ ] API настроен (CORS на бэкенде)

---

## 🆘 Поддержка Beget

Если возникнут проблемы:
- **Тикеты:** через панель управления Beget
- **Документация:** https://beget.com/ru/kb
- **Поддержка:** support@beget.com

---

## 🎉 Готово!

Ваш проект развернут на Beget! 

**URL сайта:** https://ваш-домен.ru

**Логин:** https://ваш-домен.ru/signin

---

## 📌 Полезные команды

```bash
# Пересборка проекта
npm run build

# Локальный preview собранного build
npm run preview

# Архивирование dist для загрузки
tar -czf dist.tar.gz -C dist .
```

---

## 🔗 Дополнительно

- `API_INTEGRATION.md` — документация по API
- `TROUBLESHOOTING.md` — решение проблем
- `API_SETUP_GUIDE.md` — настройка API

**Успехов с деплоем! 🚀**

