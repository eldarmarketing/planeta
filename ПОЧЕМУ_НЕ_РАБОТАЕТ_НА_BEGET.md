# 🔍 Почему на локалке работает, а на Beget нет?

## ❓ Проблема: CORS (Cross-Origin Resource Sharing)

### На локалке (работает ✅):

```
┌─────────────┐         ┌──────────────┐         ┌─────────────────┐
│  Браузер    │────────>│ Vite Server  │────────>│ API Backend     │
│             │         │ localhost:   │  Proxy  │ birson.tgapp.   │
│             │         │ 3000         │         │ online          │
└─────────────┘         └──────────────┘         └─────────────────┘
                              ↑
                        Обходит CORS!
```

**Что происходит:**
1. Браузер делает запрос на `localhost:3000/api/v1/login`
2. Vite Proxy перенаправляет на `https://birson.tgapp.online/api/v1/login`
3. **CORS не срабатывает** - потому что для браузера это один домен!
4. ✅ Всё работает!

---

### На Beget (не работает ❌):

```
┌─────────────┐                             ┌─────────────────┐
│  Браузер    │────────────────────────────>│ API Backend     │
│             │  Прямой запрос              │ birson.tgapp.   │
│             │  с ваш-домен.ru             │ online          │
└─────────────┘                             └─────────────────┘
       ↓
   ❌ CORS блокирует!
```

**Что происходит:**
1. Браузер делает запрос с `https://ваш-домен.ru`
2. На `https://birson.tgapp.online/api/v1/login`
3. **Разные домены!** → Браузер блокирует (CORS)
4. ❌ Ошибка в консоли: "CORS policy: No 'Access-Control-Allow-Origin' header"

---

## 🛠️ Решение

### Вариант 1: Настройка CORS на бэкенде (РЕКОМЕНДУЕТСЯ)

**Что нужно сделать:**

Программист должен добавить ваш домен в CORS настройки на бэкенде Rails.

**Файл:** `config/initializers/cors.rb` на бэкенде

```ruby
Rails.application.config.middleware.insert_before 0, Rack::Cors do
  allow do
    origins 'https://ваш-домен.ru'  # ← Добавить ваш домен!
    
    resource '*',
      headers: :any,
      methods: [:get, :post, :put, :patch, :delete, :options, :head],
      expose: ['Authorization'],
      credentials: true
  end
end
```

**После изменений:**
1. Перезапустить бэкенд
2. Проверить что домен добавлен
3. ✅ Всё заработает!

---

### Вариант 2: Использование демо-данных (временно)

Пока не настроен CORS, приложение может работать с демо-данными.

**В вашем проекте уже есть:**
- `STOContext` - контекст с демо-данными
- Моковые данные для клиентов, сотрудников, авто

**Чтобы активировать:**

Откройте `src/context/STOContext.tsx` и используйте локальные данные вместо API.

---

## 🔍 Как проверить что проблема в CORS?

### 1. Откройте консоль браузера (F12) на Beget

Вы увидите ошибку:

```
❌ Access to fetch at 'https://birson.tgapp.online/api/v1/login' 
   from origin 'https://ваш-домен.ru' has been blocked by CORS policy: 
   No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

### 2. Проверьте Network вкладку

- Запрос на `/api/v1/login` будет красным
- Status: (failed) или CORS error
- Response будет пустой

### 3. Проверьте что бэкенд доступен

Откройте в браузере напрямую:
```
https://birson.tgapp.online/api/v1/users
```

Если увидите JSON (даже ошибку 401) - значит бэкенд работает!
Если CORS ошибка - нужна настройка на бэкенде.

---

## 📋 Чеклист для решения проблемы

### На фронтенде (у вас):

- [x] ✅ Build собран правильно
- [x] ✅ .htaccess загружен
- [x] ✅ Файлы на Beget
- [x] ✅ Сайт открывается
- [ ] ⏳ API не работает → CORS проблема

### На бэкенде (у программиста):

- [ ] Добавить ваш домен в CORS
- [ ] Перезапустить бэкенд
- [ ] Проверить что CORS работает

---

## 🔧 Временное решение (пока не настроен CORS)

### Используйте демо-режим:

Отредактируйте `src/context/STOContext.tsx`:

```typescript
// Временно: использовать локальные данные вместо API
const USE_DEMO_DATA = true;  // ← Поставить true

if (USE_DEMO_DATA) {
  // Использовать initialData вместо API
  return initialData;
}
```

Пересоберите проект:
```bash
npm run build
```

Загрузите новый build на Beget.

---

## 📞 Что сообщить программисту?

Отправьте ему:

```
Привет! Нужно настроить CORS на бэкенде для моего домена:

Домен: https://ваш-домен.ru

Файл для изменений: config/initializers/cors.rb

Нужно добавить:
origins 'https://ваш-домен.ru'

После изменений перезапустить Rails сервер.

Спасибо!
```

---

## 🎯 Другие возможные проблемы

### 1. Белый экран

**Причина:** Не загружены JS/CSS файлы

**Решение:**
- Проверьте что папка `assets/` загружена
- Проверьте консоль (F12) на ошибки загрузки

### 2. 404 на роутах

**Причина:** Нет .htaccess или он не работает

**Решение:**
- Проверьте что `.htaccess` файл загружен
- Включите показ скрытых файлов в File Manager

### 3. Не работает вход

**Причина 1:** CORS (см. выше)

**Причина 2:** Старые токены в браузере

**Решение:**
- Откройте `https://ваш-домен.ru/clear-tokens.html`
- Очистите токены

---

## 🔄 Как будет работать после настройки CORS?

```
┌─────────────┐                             ┌─────────────────┐
│  Браузер    │────────────────────────────>│ API Backend     │
│  ваш-домен  │         CORS OK!            │ birson.tgapp.   │
│  .ru        │<────────────────────────────│ online          │
└─────────────┘        Data ✅               └─────────────────┘
```

После настройки CORS на бэкенде:
1. ✅ Вход будет работать
2. ✅ Загрузка данных будет работать
3. ✅ Все API запросы будут работать

---

## 📚 Дополнительно

### Что такое CORS?

**CORS (Cross-Origin Resource Sharing)** - это механизм безопасности браузера.

Браузер блокирует запросы между разными доменами для безопасности:
- `localhost:3000` → `birson.tgapp.online` ❌
- `ваш-домен.ru` → `birson.tgapp.online` ❌

Чтобы разрешить, нужно настроить CORS на сервере.

### Почему на локалке работает?

Vite Dev Server создаёт proxy:
- Браузер думает что запрос идёт на `localhost:3000`
- На самом деле Vite перенаправляет на `birson.tgapp.online`
- CORS не срабатывает! ✅

На продакшене нет Vite → нет proxy → CORS блокирует ❌

---

## ✅ Итого

**Проблема:** CORS блокирует запросы с Beget на бэкенд

**Решение:** Программист должен добавить ваш домен в CORS настройки

**Временно:** Можно использовать демо-данные

**Проверка:** Откройте F12 → Console → смотрите на ошибки CORS

---

**Дата:** 30 декабря 2025  
**Файл:** ПОЧЕМУ_НЕ_РАБОТАЕТ_НА_BEGET.md


